# You are given four real numbers - the coordinates of two points on a
# plane. The first two numbers are the x and y coordinates of the first point,
# and the last two numbers are the x and y coordinates of the second point.
# Output the length of the line segment bounded by the two points.

user_response = input('write the x for the first coordinate : ')
user_response_2 = input('write the y for the first coordinate: ')

x , y = (int(user_response), int(user_response_2))

user_response_3 = input('write the x for the second coordinate : ')
user_response_4 = input('write the y for the second coordinate: ')

a , b = (int(user_response_3), int(user_response_4))